<span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-{{ $revoked ? 'red' : 'green' }}-100 text-{{ $revoked ? 'red' : 'green' }}-800">
    {{ $revoked ? 'revoked' : 'valid' }}
</span>
